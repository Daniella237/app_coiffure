<div class="bg-white rounded-2xl shadow-lg overflow-hidden card-hover">
	{% if product.images is defined and product.images|length > 0 %}
		<div class="h-64 bg-gradient-to-br from-purple-100 to-indigo-100 flex items-center justify-center">
			<img src="{{ product.images[0].url }}" alt="{{ product.name }}" class="w-full h-full object-cover rounded-2xl">
		</div>
	{% else %}
		<div class="h-64 bg-gradient-to-br from-purple-100 to-indigo-100 flex items-center justify-center">
			<span class="text-6xl">🧴</span>
		</div>
	{% endif %}
	<div class="p-6">
		<h3 class="text-xl font-bold text-gray-900 mb-2">{{ product.name }}</h3>
		<p class="text-gray-600 mb-4">{{ product.description }}</p>
		<div class="flex items-center justify-between">
			<span class="text-2xl font-bold text-purple-500">{{ product.price }}€</span>
			<a href="/products/{{ product.sku }}" class="btn-primary text-sm px-4 py-2">Voir le produit</a>
		</div>
	</div>
</div>
